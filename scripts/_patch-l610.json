{
  "patches": [
    {
      "name": "fireball spell",
      "file": "shared/spells.json",
      "jsonInsert": { "file2": "shared/spells.json", "listPath": "spells", "idKey": "id", "item": {
        "id": "fireball", "name": "Fireball", "level": 3, "classes": ["wizard", "sorcerer"], "target": "burst", "range": 15,
        "save": { "ability": "dex", "onSave": "half" }, "damage": { "dice": "6d6", "type": "fire" },
        "desc": "A bright streak blossoms into a roiling flower of flame: DEX save or 6d6 fire damage, half on a success."
      } }
    },
    { "name": "playerSaveBonus helper + triggerTrap start",
      "file": "server/game/engine.js",
      "from": "function triggerTrap(state, trap, events) {\n  const p = playerEntity(state);\n  trap.triggered = true; trap.revealed = true;\n  const saveRoll = d20({ reroll1: state.character.rerollNat1 });\n  const saveMod = mod(state.character.abilities[trap.save || 'dex']);\n  const altar = getBuff(p, 'altar_blessed');\n  const success = saveRoll.natural + saveMod + (altar ? 1 : 0) >= (trap.dc || 13);",
      "to": "function playerSaveBonus(state, ability) {\n  const char = state.character;\n  let bonus = 0;\n  if (char.className === 'paladin' && char.level >= 6) bonus += Math.max(1, mod(char.abilities.cha)); // Aura of Protection\n  return bonus;\n}\n\nfunction triggerTrap(state, trap, events) {\n  const p = playerEntity(state);\n  trap.triggered = true; trap.revealed = true;\n  const char = state.character;\n  const isDex = String(trap.save || 'dex') === 'dex';\n  const evasion = char.className === 'rogue' && char.level >= 7 && isDex; // Evasion\n  let saveMod = mod(state.character.abilities[trap.save || 'dex']) + playerSaveBonus(state, trap.save || 'dex');\n  const altar = getBuff(p, 'altar_blessed');\n  if (altar) saveMod += 1;\n  let success = saveRoll.natural + saveMod >= (trap.dc || 13);\n  // Fighter Indomitable (level 9+): reroll a failed save once per long rest\n  if (!success && char.className === 'fighter' && char.level >= 9 && !char.uses.indomitable_used) {\n    char.uses = char.uses || {};\n    char.uses.indomitable_used = true;\n    const reroll = d20({ reroll1: state.character.rerollNat1 });\n    success = reroll.natural + saveMod >= (trap.dc || 13);\n    addLog(state, 'mech', 'Indomitable! ' + p.name + ' rerolls the save: d20 ' + reroll.natural + '+' + saveMod + '.');\n  }\n  const damageMult = evasion ? (success ? 0 : 0.5) : 1;"
    },
    { "name": "evasion in trap text",
      "file": "server/game/engine.js",
      "from": "const text = `A ${trap.name}! ${p.name} ${String(trap.save || 'dex').toUpperCase()} save: ${saveRoll.natural}+${saveMod} vs DC ${trap.dc} — ${success ? 'they dodge aside!' : 'they are hit!'}`;",
      "to": "const text = `A ${trap.name}! ${p.name} ${String(trap.save || 'dex').toUpperCase()} save: ${saveRoll.natural}+${saveMod} vs DC ${trap.dc} — ${success ? 'they dodge aside!' : 'they are hit!'}${evasion ? ' (Evasion)' : ''}`;"
    },
    { "name": "evasion halves damage",
      "file": "server/game/engine.js",
      "from": "  if (!success) {\n    const dmg = rollExpr(trap.damage).total;\n    const dealt = applyDamage(state, p, dmg, trap.damageType || 'piercing', events);",
      "to": "  if (!success) {\n    let dmg = rollExpr(trap.damage).total;\n    if (evasion) dmg = Math.floor(dmg / 2);\n    const dealt = applyDamage(state, p, dmg, trap.damageType || 'piercing', events);"
    },
    { "name": "L6-10 class features on sheets",
      "file": "shared/classes.json",
      "jsonClassFeatures": {
        "barbarian": [{ "level": 7, "id": "feral_instinct", "name": "Feral Instinct", "desc": "+2 to Initiative; you are rarely caught unaware." }],
        "bard": [{ "level": 10, "id": "insp_d10", "name": "Greater Inspiration", "desc": "Your Bardic Inspiration die grows to a d10 at level 10." }],
        "fighter": [{ "level": 9, "id": "indomitable", "name": "Indomitable", "desc": "Once per Long Rest, reroll a failed saving throw." }],
        "monk": [{ "level": 10, "id": "monk_dice10", "name": "Martial Arts (1d10)", "desc": "Your unarmed strikes deal 1d10 damage at level 10." }],
        "paladin": [{ "level": 6, "id": "aura_protection", "name": "Aura of Protection", "desc": "You and companions near you add your CHA modifier to saving throws (you always do)." }],
        "ranger": [{ "level": 6, "id": "roving", "name": "Roving", "desc": "Your speed increases by 10 ft." }],
        "rogue": [{ "level": 7, "id": "evasion", "name": "Evasion", "desc": "On DEX saves that deal half damage on a success, you take none on a success and half on a failure." }]
      }
    }
  ]
}
